export const environment = {
  production: true,
  apiUrl: '/api', // When deployed, API calls will be relative to the domain
  // If you're deploying to a specific domain, you might set this to:
  // apiUrl: 'https://your-production-domain.com/api'
};
